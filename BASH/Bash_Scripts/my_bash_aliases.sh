# My Aliases

# CLI
alias lsl="ls -l"
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'
alias c="clear"
alias h="history"

# PSQL
alias start-psql="sudo service postgresql start"
alias stop-psql="sudo service postgresql stop"

# SQLite3
alias start-sqlite="sqlite3"

# Git
alias ga="git add"
alias gc="git commit"
alias yeet="git push"
alias yoink="git pull"

# Ruby Bundler
alias bi="bundle install"
alias bu="bundle update"
alias be="bundle exec"
